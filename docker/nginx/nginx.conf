events { worker_connections 1024; }

http {
    upstream my-app {
        server web:8080;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://my-app;
        }
    }
}
